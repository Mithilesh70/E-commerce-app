import { UnchangedStringPipe } from './unchanged-string.pipe';

describe('UnchangedStringPipe', () => {
  it('create an instance', () => {
    const pipe = new UnchangedStringPipe();
    expect(pipe).toBeTruthy();
  });
});
